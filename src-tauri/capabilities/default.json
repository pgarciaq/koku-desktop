{
  "identifier": "default",
  "description": "Default capabilities for the main window",
  "windows": ["main", "splash"],
  "permissions": [
    "core:default",
    "opener:default",
    "dialog:default",
    "window-state:default"
  ]
}
